import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Default theme, unmodified — only reason this file exists is to load custom.css (VitePress's
// documented extension point for tweaking the default theme's own styling; see that file).
export default DefaultTheme
